import Box from "@mui/material/Box";
import { styled } from "@mui/material/styles";

export const FormWrapper = styled(Box)({
  display: "grid",
  gridTemplateColumns: "4fr 2fr 2fr 3fr",
  gap: 16,
});
