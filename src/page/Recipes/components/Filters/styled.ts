import { Box, styled } from "@mui/material";

export const TitleCategory = styled(Box)(() => ({
  display: "inline-block",
  component: "p",
  border: "1px solid rgba(5, 5, 5, 0.20)",
  padding: "14px 28px",
  borderRadius: "15px",
}));
